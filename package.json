{
  "name": "claude-tree",
  "version": "0.1.0",
  "description": "Visualize every Claude Code config visible from a directory and when it loads.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "claude-tree": "dist/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/react": "^18.3.3",
    "gray-matter": "^4.0.3",
    "ink": "^5.0.1",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "ink-testing-library": "^4.0.0",
    "tsup": "^8.1.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
